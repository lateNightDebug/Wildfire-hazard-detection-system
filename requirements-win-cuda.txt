# Windows + NVIDIA GPU (RTX 4090, Ada / sm_89), Python 3.13.
# Step 1 of 2 — install CUDA-enabled PyTorch FIRST from the CUDA index:
#   pip install -r requirements-win-cuda.txt
# Step 2 of 2 — then the core deps:
#   pip install -r requirements.txt
#
# If cu128 has no wheel for your Python at install time, switch the index-url
# below to cu130 (newest) or cu126 (conservative). All support the RTX 4090.

--index-url https://download.pytorch.org/whl/cu128
torch
torchvision
