# Core dependencies (shared, all platforms).
#
# IMPORTANT: install PyTorch FIRST, then this file, so pip does not pull a
# CPU-only torch wheel as an ultralytics dependency:
#   Windows (RTX 4090):  pip install -r requirements-win-cuda.txt
#   Mac (CPU/MPS):       pip install -r requirements-mac.txt
# ...then:               pip install -r requirements.txt

ultralytics>=8.3
sahi>=0.12.1
onnxruntime>=1.20        # ONNX detector (Azure Custom Vision export); swap in onnxruntime-gpu for CUDA
pillow>=10
numpy
opencv-python
exifread
reportlab>=4
gradio>=6
gradio_image_annotation
fastapi                  # operations console (offline web UI)
uvicorn
python-multipart         # console image uploads
pywebview                # native app window (--desktop / installed shortcut)
requests
huggingface_hub
tqdm

# Optional: GeoTIFF GPS fallback (uncomment if your drone images are orthomosaics)
# rasterio
